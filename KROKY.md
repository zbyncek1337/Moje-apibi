# Co dělat po stažení projektu

1. `git clone` = stáhni projekt
2. `cd moje-apibi` = vejdi do složky projektu
3. `dir` = zkontroluj obsah složky
4. `npm install` = připrav projekt
5. `npm start` = spusť projekt
6. otevři `http://localhost:3000`
